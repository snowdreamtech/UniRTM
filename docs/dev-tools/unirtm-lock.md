# Lockfile (unirtm.lock)

UniRTM generates a `unirtm.lock` file that pins the exact checksums and versions of the downloaded tools. You should commit this file to your repository to guarantee reproducible environments.