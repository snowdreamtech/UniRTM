# IDE Integration

UniRTM natively supports VS Code and JetBrains IDEs. Because UniRTM does not use Bash shims, IDEs can directly execute the binaries managed by UniRTM without complex shell configurations.
