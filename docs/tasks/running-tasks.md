# Running Tasks

Execute tasks via `unirtm run <task>`. Tasks can depend on other tasks. If a dependency fails, the subsequent tasks are aborted. Task output is streamed directly to stdout with Pterm styling.