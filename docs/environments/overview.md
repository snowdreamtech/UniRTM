# Environment Variables Overview

UniRTM seamlessly merges your local `.env` files and the `[env]` declarations in your `.unirtm.toml` into the shell context when you enter a directory, and purges them when you leave.
