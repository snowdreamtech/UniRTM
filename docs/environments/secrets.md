# Secrets Management

Storing passwords in plaintext `.env` files is a bad practice. UniRTM integrates with secret managers like SOPS and Age so you can safely commit encrypted environment variables to your repository.